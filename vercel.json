{
    "builds": [
      {
        "src": "api/wsgi.py",
        "use": "@vercel/python",
        "config": { "maxLambdaSize": "15mb", "runtime": "python4.0" }
      }
    ],
    "routes": [
      {
        "src": "/(.*)",
        "dest": "api/wsgi.py"
      }
    ]
}
